import { Box } from "@mui/material";

const Line = () => (
  <Box
    sx={{
      height: "5px",
      width: "100%",
      backgroundColor: "primary.main",
      borderRadius: "2px",
    }}
  />
);

export default Line;
